import { Skeleton, SkeletonGroup } from "../../../components/ui";
export function GoalsSkeleton() { return <SkeletonGroup className="goals-skeleton" label="Loading savings goals"><div className="goal-summary-grid">{Array.from({ length: 4 }).map((_, index) => <Skeleton className="goals-skeleton__summary" key={index} />)}</div><div className="goal-card-grid">{Array.from({ length: 3 }).map((_, index) => <Skeleton className="goals-skeleton__card" key={index} />)}</div></SkeletonGroup>; }
