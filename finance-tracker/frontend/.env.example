# Fintrack frontend environment template
# Vite embeds VITE_* values into the browser bundle. Never put credentials,
# private keys, or other secrets in this file.

# Backend origin, without a trailing slash. Use the public HTTPS origin in
# production (for example, https://api.example.com).
VITE_API_URL=http://127.0.0.1:8000
